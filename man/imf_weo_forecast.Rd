% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imf.R
\name{imf_weo_forecast}
\alias{imf_weo_forecast}
\title{Proyecciones del World Economic Outlook (WEO) del FMI}
\source{
\url{https://data.imf.org/en/datasets/IMF.RES:WEO}
}
\usage{
imf_weo_forecast(
  indicador = c("inflacion", "gdp"),
  last_n_obs = 6,
  countries = c("DOM", "CHN", "USA"),
  format = c("long", "wide")
)
}
\arguments{
\item{indicador}{\code{"inflacion"} (por defecto) o \code{"gdp"}. Ver \verb{@details}
para el código WEO exacto al que traduce cada etiqueta.}

\item{last_n_obs}{Número de observaciones más recientes a traer por
país (por defecto 6).}

\item{countries}{Vector de códigos de país ISO-3 del FMI (por defecto
\code{c("DOM", "CHN", "USA")}).}

\item{format}{\code{"long"} (por defecto, una fila por país y año) o
\code{"wide"} (una columna por país, indexada por año).}
}
\value{
En formato \code{"long"}, un tibble con (al menos) las columnas:
\describe{
\item{country}{\code{chr}. Código de país ISO-3.}
\item{indicator}{\code{chr}. Código WEO real al que tradujo \code{indicador}
(\code{"PCPIEPCH"} o \code{"NGDP_RPCH"}).}
\item{year}{\code{int}. Año de la observación.}
\item{value}{\code{dbl}. Valor de la observación: \% para ambos
indicadores disponibles actualmente.}
}
En formato \code{"wide"}, un tibble con columna \code{year} y una columna
adicional por cada país en \code{countries}, con el valor de \code{indicador}.
}
\description{
Descarga las últimas observaciones de inflación o crecimiento del PIB
del World Economic Outlook (WEO) del FMI para uno o más países, en
formato largo o ancho.
}
\details{
El WEO se publica dos veces al año (abril y octubre) y mezcla, en la
misma serie, años de dato real (histórico) y años de proyección, sin
que la respuesta de la API indique cuál es cuál. No encontramos un
atributo SDMX confiable para separar automáticamente lo real de lo
proyectado con el paquete \code{imf.data} (se intentó
\code{LATEST_ACTUAL_ANNUAL_DATA}, pero no existe en este dataflow). Por
ahora, quien use esta función debe determinar el corte real/proyectado
por su cuenta, por ejemplo contrastando el año contra la fecha de
publicación del vintage de WEO consultado.

\code{indicador} traduce dos etiquetas amigables a sus códigos WEO
correspondientes:
\describe{
\item{\code{"inflacion"}}{→ \code{PCPIEPCH}, inflación de \strong{fin de periodo},
\%. No es la inflación promedio (\code{PCPIPCH}), que es la cifra que
habitualmente se cita como "la" inflación anual; esta función no
da acceso a \code{PCPIPCH} por ahora.}
\item{\code{"gdp"}}{→ \code{NGDP_RPCH}, crecimiento del PIB \strong{real}, \%. Esta
función no da acceso al PIB nominal (\code{NGDPD}) por ahora.}
}
Para usar otro código WEO (\code{PCPIPCH}, \code{NGDPD}, o cualquier otro),
hay que agregar el caso correspondiente al \code{switch()} interno; con la
interfaz actual no es posible pasar un código SDMX directamente.

Solo se puede pedir un indicador por llamada; si se necesitan varios
(p. ej. inflación y PIB juntos), hay que llamar la función dos veces y
combinar los resultados con \code{dplyr::bind_rows()} o un \verb{dplyr::*_join()}
según el formato.

\code{year} se convierte a entero (antes quedaba como texto, heredado de
\code{TIME_PERIOD}).

El formato \code{"wide"} solo tiene sentido con un único indicador por
llamada: pivota a una columna por país, indexada por año.
}
\examples{
\dontrun{
imf_weo_forecast()

# Crecimiento del PIB real, formato ancho
imf_weo_forecast(indicador = "gdp", format = "wide")
}

}
