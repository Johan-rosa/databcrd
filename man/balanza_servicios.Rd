% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/balanzar_pagos.R
\name{balanza_servicios}
\alias{balanza_servicios}
\title{Balanza de Servicios - datos trimestrales}
\source{
\url{https://cdn.bancentral.gov.do/documents/estadisticas/sector-externo/documents/Balanza-de-Servicios-trimestral.xlsx}
}
\usage{
balanza_servicios()
}
\value{
Un tibble con una fila por combinación de categoría, trimestre
y naturaleza contable, con las columnas:
\describe{
\item{code}{\code{chr}. Código de la categoría tal como aparece en el
Excel: letras \code{"A."}–\code{"J."} para las líneas de servicios, o
numerales \code{"1"}, \code{"2"}, \code{"3"} para las filas
agregadas de Crédito, Débito y Saldo. Las filas de
detalle sin código propio heredan el de la categoría anterior.}
\item{naturaleza}{\code{chr}. \code{"Credito"}, \code{"Debito"} o \code{"Saldo"},
heredado hacia abajo desde la fila de encabezado de cada bloque.}
\item{concepto}{\code{chr}. Descripción de la categoría de servicio, sin
el código inicial ni guiones/dos puntos sobrantes (p. ej.
\code{"Viajes"}, \code{"Servicios financieros"}, \code{"De los cuales servicios de ZF:"}). Nota: para las filas agregadas queda \code{"Credito"},
\code{"Debito"} y \code{"Saldo"}.}
\item{fecha}{\code{Date}. Primer día del trimestre (\code{2010-01-01},
\code{2010-04-01}, ...). Ver advertencia en \verb{@details}.}
\item{year}{\code{dbl}. Año extraído de \code{fecha}.}
\item{trimestre}{\code{dbl}. Trimestre (1 a 4) extraído de \code{fecha}.}
\item{monto}{\code{dbl}. Valor trimestral reportado para esa
categoría/naturaleza, en las unidades del archivo original (USD
millones, según la convención habitual del BCRD para esta
publicación). Las celdas vacías (\code{"-"}) se interpretan como \code{0}.}
\item{monto_acumulado}{\code{dbl}. Suma acumulada de \code{monto} dentro de
cada combinación \verb{(year, concepto, code)}, ordenada por \code{fecha};
equivale al acumulado del año (year-to-date) para esa categoría, y
se reinicia en enero de cada año.}
}
}
\description{
Descarga y transforma el archivo "Balanza de Servicios trimestral.xlsx"
publicado por el Banco Central de la República Dominicana (BCRD), y lo
convierte en un tibble en formato largo (tidy) con una fila por
categoría de servicio, trimestre y naturaleza contable (crédito, débito
o saldo).
}
\details{
El archivo fuente organiza los datos en tres bloques de filas: CREDITO,
DEBITO y SALDO (I-II). Dentro de cada bloque hay filas de categoría
(bienes, transporte, viajes, telecomunicaciones, seguros, etc.) y
algunas filas de detalle sin código propio (p. ej. "De los cuales
servicios de ZF:"). Por eso \code{code} y \code{naturaleza} se completan hacia
abajo con \code{\link[tidyr:fill]{tidyr::fill()}}, heredando el valor de la última fila no
faltante.

Las fechas de las columnas trimestrales \strong{no se leen del archivo}: se
asume que la primera columna de datos corresponde a T1-2010 y se genera
una secuencia trimestral consecutiva con
\code{length.out = ncol(raw_data) - 1}. Si el BCRD inserta, elimina o
reordena columnas, o cambia el trimestre inicial, las fechas quedarán
mal alineadas sin que la función lo detecte.
}
\examples{
\dontrun{
balanza_servicios()

# Serie de viajes (turismo), solo créditos
balanza_servicios() |>
  dplyr::filter(concepto == "Viajes", naturaleza == "Credito")
}

}
