% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deuda_publica.R
\name{deuda_publica_by_sector}
\alias{deuda_publica_by_sector}
\title{Deuda pública consolidada por sector institucional}
\source{
\url{https://cdn.bancentral.gov.do/documents/estadisticas/sector-externo/documents/Deuda_Consolidada_Por_Sector_Trimestral.xlsx}
}
\usage{
deuda_publica_by_sector()
}
\value{
Un tibble con una fila por combinación de concepto y trimestre,
con las columnas:
\describe{
\item{grupo}{\code{chr}. \code{"Sector Público No Financiero"} o
\code{"Sector Público Financiero"}. \code{NA} para las filas \code{nivel == "total"}.}
\item{sector}{\code{chr}. \code{"Gobierno Central"}, \code{"Resto del SPNF"} o
\code{"Banco Central"}. \code{NA} para \code{nivel \%in\% c("total", "grupo")}.}
\item{tipo_deuda}{\code{chr}. \code{"Consolidada"}, \code{"Externa"} o \code{"Interna"}.
\code{NA} para las filas agregadas (\code{nivel \%in\% c("grupo", "sector")})
que suman ambas fuentes.}
\item{nivel}{\code{chr}. Nivel jerárquico: \code{"total"}, \code{"grupo"},
\code{"sector"}, \code{"fuente"} o \code{"detalle"}.}
\item{concepto}{\code{chr}. Descripción de la fila tal como aparece en el
Excel, sin código inicial ni paréntesis/notas al pie sobrantes.}
\item{fecha}{\code{Date}. Primer día del trimestre (\code{2013-03-01},
\code{2013-06-01}, ...). Ver advertencia en \verb{@details}.}
\item{year}{\code{dbl}. Año extraído de \code{fecha}.}
\item{trimestre}{\code{dbl}. Trimestre (1 a 4) extraído de \code{fecha}.}
\item{monto}{\code{dbl}. Monto de deuda reportado para ese concepto y
trimestre, en millones de US$.}
\item{gdp}{\code{dbl}. PIB nominal trimestral en millones de US$.}
\item{as_gdp_percent}{\code{dbl}. \code{monto / gdp * 100}.}
}
}
\description{
Descarga y transforma el archivo
"Deuda_Consolidada_Por_Sector_Trimestral.xlsx" publicado por el Banco
Central de la República Dominicana (BCRD), y lo convierte en un tibble
en formato largo (tidy) con una fila por concepto (sector, sub-sector o
fuente de financiamiento dentro de un sector) y trimestre, incluyendo el
monto en millones de US$ y su equivalente como porcentaje del PIB.
}
\details{
A diferencia de \code{\link[=deuda_publica_by_fuente]{deuda_publica_by_fuente()}}, este archivo organiza las
filas en una jerarquía de \strong{cuatro niveles}. Las filas no tienen un
código único y consistente para distinguir todos los niveles, así que
la detección combina dos señales:

\enumerate{
\item \code{"total"}: tres filas al inicio, en MAYÚSCULA SOSTENIDA
("DEUDA PÚBLICA CONSOLIDADA", "...EXTERNA CONSOLIDADA",
"...INTERNA CONSOLIDADA"), sin código ni sector asociado.
\item \code{"grupo"}: dos filas con código de letra (A./B.), agrupando
sectores institucionales (Sector Público No Financiero / Sector
Público Financiero).
\item \code{"sector"}: filas con código numérico (1./2./3.) dentro de cada
grupo (Gobierno Central, Resto del SPNF, Banco Central).
\item \code{"fuente"}: filas sin código propio que empiezan con
"Deuda Externa" o "Deuda Interna" (mayúscula inicial únicamente,
a diferencia de las filas "total"), desagregando cada sector.
\item \code{"detalle"}: una fila memo que empieza con "De los cuales"
("De los cuales: Intragubernamental"), hija de la fila
"Deuda Interna" del Gobierno Central.
}

El texto original también tiene sangría (espacios en blanco al inicio
de la celda) que en teoría distinguiría estos niveles, pero
\code{\link[readxl:read_excel]{readxl::read_excel()}} usa \code{trim_ws = TRUE} por defecto y la elimina
antes de que la función pueda medirla. Por eso el nivel se detecta por
el patrón del propio texto (mayúscula sostenida vs. mayúscula inicial,
y el prefijo "De los cuales") en lugar de la indentación. Esto es más
robusto frente al comportamiento de lectura, pero depende de que el
BCRD mantenga esa convención de mayúsculas/minúsculas en el texto.

Al igual que en \code{deuda_publica_by_fuente()}, la fila " A. SECTOR
PÚBLICO NO FINANCIERO..." tiene un espacio en blanco inicial que
"B. SECTOR PÚBLICO FINANCIERO..." no tiene; se resuelve aplicando
\code{\link[stringr:str_trim]{stringr::str_squish()}} antes de extraer el código.

\code{grupo} y \code{sector} se completan hacia abajo (con relleno acotado, no
\code{\link[tidyr:fill]{tidyr::fill()}} directo sobre toda la tabla) para que las filas de
\code{nivel} "fuente" y "detalle" hereden el sector al que pertenecen sin
que ese valor se filtre hacia las filas "total" siguientes. \code{tipo_deuda}
se deriva del texto en los niveles "total" y "fuente"; en el nivel
"detalle" se hereda de la fila "fuente" inmediatamente anterior (en
este archivo, siempre "Deuda Interna"), usando \code{dplyr::lag()} en lugar
de \code{fill()} para no arrastrar el valor más allá de esa fila.

Las fechas de las columnas trimestrales \strong{no se leen del archivo}: se
asume que la primera columna de datos corresponde a T1-2013 y se genera
una secuencia trimestral consecutiva con
\code{length.out = ncol(raw_data) - 1}. Si el BCRD inserta, elimina o
reordena columnas, o cambia el trimestre inicial, las fechas quedarán
mal alineadas sin que la función lo detecte.

El PIB nominal usado para calcular \code{as_gdp_percent} se toma de la fila
memo "Producto Interno Bruto ( Millones de USD)" en la hoja
"Sector (\%PIB)", igual que en \code{deuda_publica_by_fuente()}.
}
\examples{
\dontrun{
deuda_publica_by_sector()

# Deuda del Banco Central, solo la fuente externa
deuda_publica_by_sector() |>
  dplyr::filter(sector == "Banco Central", tipo_deuda == "Externa")
}

}
