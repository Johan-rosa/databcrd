% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imf.R
\name{imf_pcps}
\alias{imf_pcps}
\title{Precios de materias primas del FMI (PCPS)}
\source{
\url{https://data.imf.org/en/datasets/IMF.RES:PCPS}
}
\usage{
imf_pcps(
  indicadores = default_pcps_indicators,
  data_transformation = "INDEX",
  start_period = "2019-01"
)
}
\arguments{
\item{indicadores}{Vector de códigos de indicador PCPS (ver
\code{\link[=imf_pcps_catalogo]{imf_pcps_catalogo()}} para la lista completa). Por defecto,
\code{default_pcps_indicators}: una selección curada de 16 índices y
precios de alimentos, bebidas, cereales y fertilizantes.}

\item{data_transformation}{Código SDMX de transformación de datos.
\code{"INDEX"} por defecto (series en forma de índice); otras
transformaciones disponibles dependen del indicador.}

\item{start_period}{Periodo inicial de la consulta, como cadena
\code{"YYYY-MM"} (p. ej. \code{"2019-01"}). Se convierte con \code{as.character()}
antes de enviarse a la API.}
}
\value{
Un tibble con las columnas:
\describe{
\item{id}{\code{chr}. Código del indicador PCPS.}
\item{date}{\code{Date}. Primer día del mes de la observación.}
\item{value}{\code{dbl}. Valor de la observación, en la transformación
solicitada (\code{data_transformation}).}
\item{indicador_name}{\code{chr}. Nombre descriptivo del indicador,
obtenido de \code{\link[=imf_pcps_catalogo]{imf_pcps_catalogo()}}. \code{NA} si el código no aparece en
el catálogo actual.}
}
}
\description{
Descarga series mensuales del Primary Commodity Price System (PCPS) del
FMI para uno o más indicadores, y las une con sus nombres descriptivos
obtenidos de \code{\link[=imf_pcps_catalogo]{imf_pcps_catalogo()}}.
}
\details{
La consulta siempre filtra \code{COUNTRY = "G001"} (agregado mundial): PCPS
es un sistema de precios internacionales de referencia y no reporta
series por país, así que este es el único código de país que existe
para este dataflow. Se deja explícito en el filtro en vez de depender
del comportamiento por defecto de \code{\link[imf.data:get_data]{imf.data::get_data()}} (que ya
devuelve únicamente \code{"G001"} si se omite el filtro), para que la
consulta sea autoexplicativa y no dependa de un default no documentado.

El catálogo (\code{imf_pcps_catalogo()}) se descarga en cada llamada a esta
función; implica una llamada de red adicional solo para obtener los
nombres de los indicadores.
}
\examples{
\dontrun{
imf_pcps()

# Solo el precio del petróleo Brent, desde 2015
imf_pcps(indicadores = "POILBRE", start_period = "2015-01")
}

}
