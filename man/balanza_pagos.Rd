% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/balanza_pagos.R
\name{balanza_pagos}
\alias{balanza_pagos}
\title{Balanza de Pagos - datos trimestrales}
\source{
\url{https://cdn.bancentral.gov.do/documents/estadisticas/sector-externo/documents/bpagos_trim_6.xlsx}
}
\usage{
balanza_pagos(
  force_bind_by_position = FALSE,
  filtro_codigo = NULL,
  filtro_naturaleza = NULL,
  filtro_concepto = NULL,
  filtro_cuenta = NULL,
  filtro_nivel = NULL,
  solo_hojas = FALSE
)
}
\arguments{
\item{force_bind_by_position}{\code{logical}. Si \code{FALSE} (por defecto) y
se detectan conceptos cuyo texto crudo cambio respecto al
catalogo, la funcion aborta. Si \code{TRUE}, solo advierte e igual
devuelve el resultado.}

\item{filtro_codigo}{\code{character}. Vector de \code{code} a conservar
(match exacto), p. ej. \code{c("1.1.1.1", "1.1.1.2")}. \code{NULL} (por
defecto) no filtra.}

\item{filtro_naturaleza}{\code{character}. Vector de \code{naturaleza} a
conservar (\code{"Credito"}, \code{"Debito"}, \code{"Saldo"}). \code{NULL} no filtra.}

\item{filtro_concepto}{\code{character}. Vector de patrones a buscar en
\code{concepto} (match parcial, insensible a mayusculas/minusculas;
basta con que coincida uno de los patrones). \code{NULL} no filtra.}

\item{filtro_cuenta}{\code{character}. Vector de \code{cuenta} (nivel 1) a
conservar, p. ej. \code{"Cuenta Financiera"}. \code{NULL} no filtra.}

\item{filtro_nivel}{\code{integer}. Vector de \code{nivel} a conservar, p. ej.
\code{1:2} para quedarse solo con el resumen. \code{NULL} no filtra.}

\item{solo_hojas}{\code{logical}. Si \code{TRUE}, descarta las filas de
subtotal (aquellas cuyo \code{code} aparece como \code{code_padre} de otra
fila), dejando solo las categorias mas finas. Util para sumar
\code{monto} sin duplicar cifras. Por defecto \code{FALSE}.}
}
\value{
Un tibble en formato ancho con una fila por cuenta/subcuenta
(igual a \link{catalogo_balanza_pagos}, sin las columnas \code{nota} y
\code{concepto_fuente}) y una columna adicional por cada trimestre
publicado (nombrada con la fecha de inicio del trimestre, p. ej.
\code{"2010-01-01"}), en millones de USD, filtrado segun los
argumentos \verb{filtro_*}/\code{solo_hojas} provistos.
}
\description{
Descarga y transforma la hoja \code{BOP_TRIM} del archivo de Balanza de
Pagos publicado por el Banco Central de la Republica Dominicana
(BCRD), y la une contra \link{catalogo_balanza_pagos}, que trae el codigo
jerarquico, nivel, naturaleza contable y cuenta de nivel 1 de cada
fila.
}
\details{
A diferencia de \code{\link[=balanza_servicios]{balanza_servicios()}}, aqui el \code{code}/\code{naturaleza}
de cada fila no se extraen con expresiones regulares: se pegan por
POSICION contra \link{catalogo_balanza_pagos}, porque las filas de
detalle (p. ej. "Exportaciones", "Credito") no traen ningun codigo
propio en el texto y no hay forma confiable de inferirlas solo del
texto. Esto exige que el archivo mantenga las mismas 57 filas de
datos, en el mismo orden, que cuando se construyo el catalogo; si
el conteo de filas no coincide, la funcion aborta con un mensaje
explicito en vez de pegar datos desalineados en silencio.

Como salvaguarda adicional, se compara el texto crudo leido en cada
corrida (\code{og_concepto}) contra el texto crudo capturado al construir
el catalogo (columna \code{concepto_fuente} de \link{catalogo_balanza_pagos}),
usando solo una limpieza de espacios (\code{stringr::str_squish()}), sin
reconstruir el texto normalizado del catalogo. Esto evita falsos
positivos: la comparacion solo falla si el BCRD cambio el texto
fuente, no cuando el catalogo tiene un estilo distinto (acentos
removidos, abreviaturas expandidas, notas al pie separadas).

Si se detectan conceptos con texto distinto, la funcion imprime la
tabla de diferencias y aborta, a menos que se llame con
\code{force_bind_by_position = TRUE}, en cuyo caso solo emite un warning
y continua (bajo responsabilidad de quien llama, ya que no se puede
garantizar que el orden de las filas siga correspondiendo al
catalogo).

Los filtros (\verb{filtro_*}, \code{solo_hojas}) se aplican al final, despues
de unir los datos crudos contra el catalogo, y son todos opcionales
(\code{NULL} o \code{FALSE} = sin filtrar). Se pueden combinar libremente.

\code{concepto} NO es unico en el catalogo: valores como \code{"Credito"},
\code{"Debito"}, \code{"Otros"}, \code{"Nacionales"}, \code{"Zonas Francas"}, \code{"Remesas Familiares"}, \code{"Otras Transferencias"}, \code{"Inversion de Cartera"} y
\code{"Activos de Reservas"} aparecen repetidos bajo distintas cuentas
padre. \code{filtro_concepto} hace match parcial insensible a mayusculas
(via \code{stringr::str_detect()}), asi que por si solo puede devolver
varias categorias no relacionadas; combinalo con \code{filtro_cuenta} o
\code{filtro_codigo} para desambiguar.
}
\examples{
\dontrun{
balanza_pagos()

# Solo la Cuenta Financiera, sin subtotales
balanza_pagos(filtro_cuenta = "Cuenta Financiera", solo_hojas = TRUE)

# Categorias de credito en Ingreso Primario, resumen (nivel 1-3)
balanza_pagos(
  filtro_cuenta = "Cuenta Corriente",
  filtro_naturaleza = "Credito",
  filtro_nivel = 1:3
)
}

}
\seealso{
\link{catalogo_balanza_pagos}, \code{\link[=balanza_servicios]{balanza_servicios()}}
}
