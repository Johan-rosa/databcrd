% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/china_data.R
\name{china_tree_node}
\alias{china_tree_node}
\title{Navigate the NBS data-portal folder tree}
\usage{
china_tree_node(parent_id = NULL, code = 19)
}
\arguments{
\item{parent_id}{Character. The \code{id} of the parent node to list children
for. Use \code{NULL} (the default) to fetch the root of the tree.}

\item{code}{Integer/character. Which panel of the site to query: \code{1} =
"Browse topics" / "advanced search" panel, \code{19} = "Visualized" panel
(the default). During development, \code{code = 19} was the panel that
reliably contained the Price Index section, but this is not guaranteed
to remain stable if the site changes.}
}
\value{
A tibble with (when available) columns \code{level}, \code{parent_id},
\code{id}, \code{name}, and \code{dt} -- one row per child node. Returns a 0-row
tibble if the node has no children.
}
\description{
The NBS data portal organizes its data series in a tree of "nodes"
(folders and leaves). This function queries one level of that tree given
a parent node id, and is the building block used to locate specific
indicators (e.g. CPI) by walking down from the root.
}
\examples{
\dontrun{
root <- china_tree_node()
sections <- china_tree_node(root$id)
}
}
