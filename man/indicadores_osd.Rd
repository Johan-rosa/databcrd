% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicadores_osd.R
\name{indicadores_osd}
\alias{indicadores_osd}
\title{Obtener indicadores de Otras Sociedades de Depósito (OSD)}
\usage{
indicadores_osd(
  filtro_variable = NULL,
  filtro_nivel_tipo = NULL,
  filtro_sector = NULL,
  filtro_moneda = NULL,
  filtro_entidad = NULL
)
}
\arguments{
\item{filter_by_variable}{Vector de caracteres opcional. Filtra por
una o más de las siguientes categorías:
\itemize{
\item \code{"Activos externos"}
\item \code{"Pasivos externos"}
\item \code{"Inversiones"}
\item \code{"Préstamos"}
\item \code{"Depósitos"}
\item \code{"Composición depósitos"}
\item \code{"Tasa de cambio"}
}
\code{NULL} (por defecto) no aplica filtro.}

\item{filter_by_sector}{Vector de caracteres opcional. Filtra por
uno o más de los siguientes valores:
\itemize{
\item \code{"Total"}
\item \code{"Sector público"}
\item \code{"Sector privado"}
\item \code{"Sociedades financieras"}
\item \code{"No residentes"}
\item \code{"Depósitos transferibles"}
\item \code{"Otros depósitos"}
\item \code{"Valores distintos de acciones"}
}
Aplica solo a las variables \code{"Préstamos"}, \code{"Depósitos"} y
\code{"Composición depósitos"}; para el resto de variables esta columna
es \code{NA} (ver "Nota sobre valores \code{NA}" más abajo).}

\item{filter_by_moneda}{Vector de caracteres opcional. Filtra por
uno o más de los siguientes valores: \code{"Total"}, \code{"DOP"}, \code{"USD"}.
Aplica solo a \code{"Préstamos"} y \code{"Depósitos"}; para el resto de
variables esta columna es \code{NA}.}

\item{filter_by_entidad}{Vector de caracteres opcional. Filtra por
uno o más de los siguientes valores:
\itemize{
\item \code{"General"} (agregado, no desglosado por entidad)
\item \code{"Bancos múltiples"}
\item \code{"Resto OSD"}
}}

\item{filter_by_nivel}{Vector entero opcional. Filtra por nivel de
agregación:
\itemize{
\item \code{1} — Total agregado
\item \code{2} — Por sector
\item \code{3} — Por moneda
\item \code{4} — Por entidad
}}
}
\value{
Un tibble en formato largo con columnas \code{row_id}, \code{indicador},
\code{date}, \code{value}, \code{variable}, \code{sector}, \code{moneda}, \code{entidad}, \code{nivel},
\code{nivel_tipo}.
}
\description{
Descarga la serie de indicadores de OSD publicada por el Banco Central
de la República Dominicana, la transforma a formato largo (tidy) y la
enriquece con los metadatos de clasificación provenientes de
\code{detalles_indicadores_osd}.
}
\section{Nota sobre valores \code{NA} en \code{sector} y \code{moneda}}{

Variables como \code{"Activos externos"}, \code{"Pasivos externos"},
\code{"Inversiones"} y \code{"Tasa de cambio"} no tienen desglose por sector
ni por moneda, por lo que esas columnas quedan en \code{NA} para esas
filas. Como \code{NA \%in\% x} siempre evalúa a \code{FALSE} (nunca \code{NA}),
aplicar \code{filter_by_sector} o \code{filter_by_moneda} excluye
automáticamente esas variables del resultado. Si quieres esas
variables junto con un filtro de sector/moneda, combina con
\code{filter_by_variable} en la misma llamada, o filtra en dos pasos.
}

\examples{
\dontrun{
indicadores_osd()

indicadores_osd(
  filtro_variable = "Préstamos",
  filtro_sector = "Sector privado",
  filtro_moneda = "USD"
)

indicadores_osd(filter_by_entidad = c("Bancos múltiples", "Resto OSD"))
}

}
